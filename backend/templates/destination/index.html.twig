{% extends 'base.html.twig' %}

{% block title %}Destination index
{% endblock %}

{% block body %}
	<h1>Destination index</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Country</th>
				<th>City</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for destination in destinations %}
				<tr>
					<td>{{ destination.country }}</td>
					<td>{{ destination.city }}</td>

					<td class="d-flex  align-items-center gap-3">
						<a class="btn btn-info" href="{{ path('app_destination_show', {'id': destination.id}) }}">show</a>
						<a class="btn btn-warning" href="{{ path('app_destination_edit', {'id': destination.id}) }}">edit</a>
						{{ include('destination/_delete_form.html.twig') }}

					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="5">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<a class="btn btn-primary" href="{{ path('app_destination_new') }}">Create new</a>
{% endblock %}
